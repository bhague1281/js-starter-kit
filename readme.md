#Front-End JS Starter Kit

##Setup

* Clone repository
* Run `npm install` for dependencies
* Run `npm start` to start Webpack + BrowserSync

##The Useful Dependencies

* Webpack
  - ES6
  - ESLint
  - PostCSS + AutoPrefixer
  - SASS
  - `window.fetch`
